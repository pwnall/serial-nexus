{
  "name": "serial-nexus-web-ui-tests",
  "private": true,
  "version": "0.0.0",
  "license": "MIT OR Apache-2.0",
  "description": "Headless-Chromium behaviour suite for the serial_nexus web console (design §15.37). Dev-time only: the console's own assets stay vendored and unbundled, and nothing here ships.",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
