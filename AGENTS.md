# AGENTS.md — operating manual for serial_nexus implementation sessions

## §1 What this repository is
A Rust workspace implementing the serial_nexus design: `serial-nexus-daemon` (lib `serial_nexus_daemon`), `serial-nexus-ctl`, `serial-nexus-web`, `serial-nexus-doctor`, `serial-nexus-sim`, the `serial-nexus-itest` harness, and the libraries `serial_nexus_core`, `serial_nexus_rpc`, `serial_nexus_sys`, `serial_nexus_codec_api`. Directory names stay short; Cargo `name` fields carry the family prefix (design §15.40). The normative documents are the current design/plan pair named in §2; §ref numbers always point at the current design. Older doc versions, frozen reviews, and remediation ledgers live in `docs/historical/` and are records, not law.

## §2 Current state, and the claims discipline
The normative pair is `43-design-claude-fable-v17.md` + `44-implementation-plan-claude-fable-v17.md`, both directly under `docs/`; when a generation lands, the superseded pair moves to `docs/historical/` and this line and README's documentation index — the two places that name the pair by filename as normative — are bumped in the same commit, together with the four filename-keyed code sites: `itest/tests/meta_names.rs`'s §15.41 ban-statement allowance and citation-gate scoping pair, and `itest/tests/meta_derive.rs`'s `PLAN`/`DESIGN` consts (this list undercounted everything past the first until the v17 landing). A meta-gate fails if either entry point goes stale. **The v16 generation landed 2026-08-10**: a full rewrite optimized for new developers, produced by a digest fan-out over the v15 pair, the implementation notes, and the review record (17 must-preserve inventories), written to a disposition map that keeps every §15/§16 entry number and every plan §3 rule and §18 item number stable, and adversarially verified by ten independent checkers before landing — the alignment obligation (design front matter, rewrite invariant 2) executed for this generation. Nothing recorded as declined or refuted was silently reversed; overturned declines are marked OVERTURNED at their entries. **The v17 revision landed 2026-08-12**: an edit of v16 under rewrite invariant 2 (authored from a thirteen-agent input digest, adversarially verified against the v16→v17 diff) that specifies the pattern wait — `tap.wait`, §10 and §15.56, carried as plan §18 item 47 — folds the notes §3.76–§3.81 record at its sources, repairs five figure and citation defects where they stood, appends plan §3 rule 22, and files items 47–55; every entry, rule, and item number is unchanged. **Item 47 was then executed the same day** (notes §3.83): `tap.wait` answers on the wire, and §10's pattern-wait subsection is settled system like the rest of §1–§14. **The v17 alignment pass against the tree ran 2026-08-12** (notes §3.84, §3.85): eleven section readers against the code with one adversarial verifier per claim, fifteen deviations confirmed and repaired and eight refuted; ten ledger items closed, two of them by *declining* with the alternative measured rather than argued (45, 27); three new defects filed and executed as items 56–58. It left **exactly one design-ahead-of-tree surface**, deliberately and under AGENTS §5's amend-first order: the `actual_baud` read-back in a serial node's state (§7.1 clause 7, decided at §15.58), carried as plan §18 item 41. One filed clause deviated with the reason recorded — the CLI's exit codes are grep's 0/1/2, not the item's originally-filed numbering, because `2` was already clap's usage error and the cited doctor precedent runs the other way. The design's §1–§14 and §17 are the system as settled, §15–§16 the status-headed decision record; the plan carries the harness doctrine (plan §3, rules 1–22), the testing strategy, and the work ledger. **The plan's Status table is the single home of every current-era measured figure — a figure is quotable only with its scope column — and plan §18 is the canonical enumeration of open work** (of the v15 ledger's fifteen items, 1, 2, 3, 5, 6, 7, 9, 10 and 11 are executed and 4-residual, 8 and 12–15 remain; of the items filed at the rewrite, **23, 32, 33, 34, 35, 37, 39, 40, 43 and 44 were executed 2026-08-12** — notes §3.75 — item 18's suite half is discharged by the same session's CI macOS run (notes §3.76), **item 47 was executed 2026-08-12** — notes §3.83. The 2026-08-12 alignment pass against the tree then executed **21, 27, 29, 36, 38, 45, 46, 48, 49, 51, 52, 53, 54, 55 and 56–58** — notes §3.84–§3.86, with 27 and 45 closed as *declines* whose alternatives were measured, and 55 carrying two clauses blocked on a file-ownership conflict into item 59. **Still open: 4-residual, 8, 12–20, 22, 24, 25, 26, 28, 30, 31, 41, 42, 50 and the new 59** — the platform-blocked ones (4, 8, 12, 13, 15, 18, 20 need a Mac or a second kernel; 28 needs DTR wired; 31 needs a root box) and the rig-session ones (16, 17, 19, 22, 26, 30)). **The v16 pair's alignment obligation was executed against the *tree* on 2026-08-12** (the landing had executed it against the documents): 460 normative clauses checked, nineteen deviations confirmed and repaired, two refuted and recorded — two product defects, three gate holes, two design amendments (§15.54), and twelve prose-outran-the-tree corrections. Not one was a rule the rewrite dropped. Kernel claims cite committed `docs/doctor/` artifacts (§16.13); the probe-set era record and the comparability ladder are §13's. **The roster is P1–P16 and the current era is `4317ea5ac187f506`** (opened 2026-08-13 by P16's arrival together with P15's widened `question` — one boundary, two changes, deliberately; notes §3.89/§3.90). Earlier the same day two `field_set` moves closed nothing, per §13's era law clause 4, so their artifacts stay era-mates of the rows below them while nothing diffs across the `probe_set` row. The current Linux figure is **1004 passing · 0 failed · 7 ignored** at default CI scope with `--no-fail-fast`, `SNX_EXEC_CODEC=required` and `SNX_TLS=required` (2026-08-14, the session that validated the macOS tree on Linux — notes §3.101; the suite has been fully green since 2026-08-12, item 46's rebuilt guard reading 0.0792 %/fd under suite parallelism against its 0.1274 %/fd ceiling, and every delta is decomposed per target in the Status table — the +5 over the 999 row is quotable because **both ends were measured in the same session**, the macOS session's own tree reading 1003 here before anything changed). **Runs are orphan-clean by assertion, not by observation:** every `Daemon` sweeps its process group at drop and panics naming survivors (item 65) and **1004 · 0 · 7** on the **documented rig lane** — and as of 2026-08-14 that lane carries `SNX_RIG_FLOW=required` for the first time in the Status table (`SNX_CROSSOVER`/`SNX_REPLUG`/`SNX_TLS`/`SNX_RIG_FLOW`/`SNX_EXEC_CODEC` all `required`, dropping only `SNX_WEB_UI` on a box with no `node` — so it is **not** the fully-spelled lane and is not quotable as one, and "first ever to set that word" is not establishable either: see the row's own caveat), because **the operator re-cabled the bench from 3-wire to 5-wire and P5 confirms it** (`rts_a_to_cts_b=true rts_b_to_cts_a=true`, all six DTR crossings `false`, 3 of 3, committed at `docs/doctor/linux-7.0-2026-08-14-b58a1c4-*`; notes §3.102, §15.52's re-cable annotation) — every hardware test passing, the blessed `grant` verb exercised across each re-enumeration, and the self-skip count falling to **3**, the two names that left it being exactly the two `skip_no_rig_flow` callers. **The adapter pair is new to the record** (`BH00L4KU` ↔ `BH00LW9U`), so nothing is diffed across the 3-wire rows as though only the cable moved. **DTR is still unwired on this third cabling, so item 28 stays blocked**, and the suite's DTR negative control stays a control. **No gate in this tree can tell a 5-wire bench from a 3-wire one** — the jq handshake clause is presence-only by design (plan §3 rule 14) — so the wiring claim rests on the committed P5 artifact and the two `rts-cts` tests under `SNX_RIG_FLOW=required`, both of which were fail-first proven against this bench. **Items 74–77 — the four defects that session found in those very guards — were executed 2026-08-15** (notes §3.103): the handshake precondition now measures both directions (a **half-crossed** bench skips, and hard-fails under `SNX_RIG_FLOW=required`, which is a miswiring rather than §15.52's legitimate 3-wire answer), the stall test's control latency is **measured** at 20.35 ms rather than remembered and its margin asserted structurally, and a vacuous doctor loop is replaced by the signatures that actually carry its property. **One of the four did not survive testing and is the most useful of them:** item 76's predicted unprimed failure **did not reproduce, 5 of 5**, while the underlying §3.70 swallow *did* reproduce on this pair **1 of 3** through a 32768-byte transfer — so the primer stays on measured grounds but the item's stated failure mode is unproven here and is not quotable. **An underpowered negative is not a refutation**; what separated them was measuring the mechanism rather than the symptom. **The earlier 1004 pair are equal at the same tree in the same session, which is item 30's dual-scope measurement; no delta is derived across the scopes**, and the 5-wire lane is a same-tree cross-session reproduction rather than a second dual-scope one. **A re-bless is expected whenever `sys/` changes** — `devprep` links it, so `install` rewrites the binary and the kernel strips the capability; that is §15.45's design, not a defect. That lane also discharged plan §18 item 67's owed remainder: the `xon-xoff` rig guard took its **`Honoured`** arm on real hardware, an arm that had executed on no machine anywhere, so §15.61's refusal is proven to *discriminate* against hardware on both kernels with the same two adapters and cable. and macOS is **896 · 0 · 6** at whole-workspace scope on the CI arm64 runner (2026-08-13, run 31657666919) and **961 · 0 · 7** at default CI scope on the **x86_64 Mac rig box** (2026-08-13, `ad4dfb2`; notes §3.93–§3.100) — three macOS machines, none substituting for another (plan §18 item 18), and **a macOS default-scope figure is not comparable to a Linux one test-for-test**: the rig box self-skips on the order of a hundred device- and Linux-gated tests against Linux's ~14 at the same scope, the rig being attached but `SNX_CROSSOVER_A`/`_B` unexported. **Do not quote that skip count precisely, on either platform**: `grep -c '^SKIP'` is unstable under `--nocapture` because the suite's parallel binaries interleave writes and break the line anchor (two adjacent trees on one Mac read 105 and 102, with every "missing" line present unanchored) — a second instance of notes §3.78's class, not a contradiction of it. **And it is not a macOS property**: two default-scope runs of one Linux tree in one session read 13 and 14, and extracting *names* rather than counting anchors yields 13 from each with **different** sets — a splice can break the name as well as the anchor, so the "missing" line is absent in any form. The union is 14 and is itself a floor, so the **13** every Linux row carried is an undercount (notes §3.101). The figure only needs to separate a rig lane (5) from a default-scope run (~14), and that gap is an order of magnitude clear of the noise. The **whole rig binary is green on macOS for the first time — 7 of 7** — after item 70, and `p9_pty_collapse`'s anti-spin guard now **runs** there rather than self-skipping (item 12: `serial_nexus_sys::process_cpu_nanos` is the portable CPU source the tree said did not exist). **Item 12's central prediction is refuted** — the ungated `|| closed` plant moves the reading on neither kernel (planted 1.81/1.88/1.81 %, unplanted 1.87/1.88/1.81 %) — and item 13's Mac number is measured: 9.91 % of a core at 32 idle tty fds, marginal 0.2578 %/fd against Linux's recorded 0.0728. **`p4_free_for_all` on Darwin is closed by its own pre-registration** (item 20, notes §3.97): the frozen 12-of-12 *failure* now reads **12 of 12 passing** on the Mac rig, which is notes §3.56's "Held" arm — so §15.48's *a stall or a loss, not separated* separates, and it was a **loss** of §3.29's class, with the confound that the adapter pair and the tree both moved between the two measurements recorded beside it. **The era's macOS doctor capture landed 2026-08-13** — six artifacts (`macos-24.6.0-2026-08-13-b346188-{passive-1..3,tier3,tier3-2,tier3-3}.json`), one clean build, `jq -e -f expectations/macos.jq` executed against all six, taken on the *same adapters and cable* as the current Linux rows so that pair is the first Darwin/Linux comparison holding `probe_set`, hardware and wiring fixed. It answered three pre-registrations: `SlaveWitness::prove_open` is **unsound on Darwin** (item 66 filed), `IOSerialFamily` **drops `IXON`/`IXOFF`** exactly as it drops `CRTSCTS`, meeting item 14's conditional decline's stated condition (item 67 filed), and a reader arriving inside a pts close-wait **ends** it (14 µs against 600368) — and all are quotable only from the plan's Status table with their scopes. **The macOS doctor jq gate executed green for the first time in that run** (plan §18 item 48's measurement half); **its placement half landed 2026-08-12** — the gate now runs immediately after the build and ahead of the tests, and both steps key on the build's outcome so neither can hide the other. **Do not quote "zero SKIP lines" beside either**: `cargo test` captures a passing test's stderr, so that count is 0 whatever skipped unless the run passed `--nocapture` (notes §3.78; the measured Linux figure is 12). **The macOS session's tree was validated on Linux 2026-08-14** (notes §3.101, plan §18 item 72): its code survives this kernel intact, and what did not survive is a family of *claims* — §15.61's refusal extension landed in the daemon and in P15's `does_not_license` cell but not in the three arms of `p15_soft_note`, so a report carried the corrected cell and the stale prose in the same JSON, the arm that prints on the Linux rig reading "no pre-check consults it and no config is refused on it". **A green suite certified it because a guard pinned the *decision* rather than the mechanism** — a new register of §3's tell, and one no gate could reach, since `expectations/*.jq` never reads `.consequence`. Also repaired: the pre-check's cost statement, still bounding the DTR/RTS drop to `rts-cts` nodes after §15.61 put every `xon-xoff` node on that path; and notes §3.94's "both new guards run and pass on Linux", one being `#[cfg(not(target_os = "linux"))]` — item 66's step 4 has **no Linux trigger at all**, which is acceptable for a measured reason now guarded: while any fd on a pts is open Linux will not reallocate that index, so the witness's path check always answers first. One gap is **filed rather than fixed** (item 73): P15 cross-checks the shipped predicate for the *hardware* mode only, and §15.61 turned the software cell from reported-never-judged into a reading that refuses an operator's `load` — so the two implementations are now the shape §7.1 clause 2 forbids, which is item 56 one mode over. Every headline claim here (suite count, gate set, verified platforms) must match `docs/implementation-notes.md` and the plan's Status table — reviews check this file against reality, and a stale claim is a defect. Update this section in the same commit as the change it describes.
## §3 Gates (all must be green before "done")
`cargo build --workspace --locked` · `cargo test --workspace --locked` · `cargo fmt --all --check` · `cargo clippy --workspace --all-targets -- -D warnings` **and** the minimal-daemon clippy · `cargo deny check licenses bans sources` · macOS cross-check (`--exclude serial-nexus-web`) · the Apple cross-check, which the Linux lane runs on **both** triples because neither is a superset of the other — each catches the arch-specific code written on the other architecture, and `cargo build` sees neither, compiling no test target (notes §3.71). It is a **lint** cross-check as of 2026-08-12 (plan §18 item 54): `cargo clippy --target {aarch64,x86_64}-apple-darwin --workspace --exclude serial-nexus-web --all-targets -- -D warnings`, plus the minimal-daemon spelling on each triple. Clippy subsumes `check`, and the class it adds was caught by nothing in CI — lints ran on the Linux lane only, so a helper whose one caller is `#[cfg(target_os = "linux")]` is dead off Linux and nobody looked. Measured against a planted instance: Linux clippy exit 0, `cargo check --target aarch64-apple-darwin` exit 0, `cargo clippy --target aarch64-apple-darwin` exit 101 naming the unused fn · **the same check runs in the other direction from a Mac, and a Mac session should run it before pushing**: `cargo check -p <crate> --all-targets --target x86_64-unknown-linux-gnu` works for every crate except `serial-nexus-web` (whose `ring` needs a Linux C toolchain), and it catches the mirror class — a call site inside `#[cfg(target_os = "linux")]` that a Mac cannot compile. Not a coverage hole (CI's Linux lane compiles that code anyway) but a turnaround one: measured 2026-08-13, when a rename left a Linux-gated call to a deleted function that nothing local could see, `cargo build` compiling no test target (notes §3.98) · the doctor gate, spelled as CI runs it (plan §3 rule 21 — one run, gate on the JSON twin): `serial-nexus-doctor --markdown --json-out <report.json> > <report.md>` then `jq -e -f expectations/linux.jq <report.json>` (the macOS lane gates on `expectations/macos.jq` the same way) — **the `-e` is load-bearing and this line omitted it until 2026-08-07**: without it jq prints `false` and exits **0**, so the command reads as a gate and asserts nothing (measured against a deliberately-gutted report: exit 0 without `-e`, exit 1 with it). CI always had it right, so no lane was ever blind; only this line was — **but the macOS half of that sentence needed a second correction on 2026-08-12**: the gate had not *executed* in any recent run, because the test step ahead of it failed and a failed step skips the rest of the job, and both lanes were piping the doctor into `tee`, which discards its exit status (1 on any `unsupported`, 2 on a report it could not produce). A red check upstream of a gate hides the gate, and `--no-fail-fast` buys that only inside `cargo test` (notes §3.77) · the Playwright gate (self-skips without node) · the meta-gates, including the retired-name grep (§15.40) and the consumer-context grep (§15.41), both in `itest/tests/meta_names.rs`. The **rig lane** is by hand on the crossover box and is not part of the default gate set (CI has no adapters and the tests correctly self-skip): `SNX_CROSSOVER=required SNX_REPLUG=required SNX_TLS=required SNX_RIG_FLOW=required SNX_WEB_UI=required SNX_CROSSOVER_A=… SNX_CROSSOVER_B=… SNX_REPLUG_DEV=/dev/serial/by-id/… SNX_REPLUG_DEV_B=/dev/serial/by-id/… cargo test --workspace --no-fail-fast` (`SNX_WEB_UI=required` joined the documented lane spelling at the v16 landing — notes §3.68's session gated the browser lane under it (the 835 once attributed to that session is unreconciled with its verbatim record — plan Status row), and this line had omitted it; `SNX_REPLUG_DEV_B` joined it 2026-08-12 — notes §3.75 — because without it `identity_survives_a_replug_that_renumbers_the_tty` self-skips and `SNX_REPLUG=required` turns that skip into a hard failure, so the documented lane could not go green on the rig it describes) (`SNX_TLS=required` is the third instance of the one `required` mechanism, covering `web_tls_round_trip`'s two silent skips — no `curl`, and a sandbox that cannot bind `0.0.0.0:0` with `--tls`; plan §3 rule 11, notes §3.57. `SNX_RIG_FLOW=required` is the fourth, and the first whose precondition is *measured* rather than declared: it covers the two `rts-cts` end-to-end tests, which skip on a **3-wire** bench — §5's own stated design assumption, so a rig that answers no is legitimate and `SNX_CROSSOVER=required` deliberately does not redden it (design §15.52, notes §3.63). On macOS `SNX_CROSSOVER` additionally *enables* the `cu.usbserial` scan, which no longer fires unasked — plan §18 item 5) (`--no-fail-fast` per §6 — this is platform validation; **drop `SNX_RIG_FLOW=required` on a bench P5 measures as 3-wire, and `SNX_WEB_UI=required` without `node`** — both are legitimate answers and forcing them fails the lane for the operator's cabling or toolchain, notes §3.80), preceded once by `scripts/bless` (build + install + one `setcap`; `--all` for both profiles). CI enumerates loops from tools (`cargo fuzz list`), never hand-kept lists; meta-gates assert *execution*, not file existence, and a scanning gate proves its **matcher** as well as its walker — plant the violation in every spelling it claims to cover. **The tell for a gate that asserts nothing: its passing output is identical to its not-running output.** Three instances are recorded — `jq` without `-e`, a gate whose step never ran because the step ahead of it was red (notes §3.77), and a SKIP-line grep whose subject `cargo test` captures before it can be grepped (notes §3.78). **Two further registers of the same tell, both found 2026-08-14** (notes §3.101): a gate that asserts the **previous** answer — a guard pinning a *decision* rather than a *mechanism*, which a decision change silently converts into the thing holding the defect in place; and a gate whose assertion is strictly **weaker than the comment above it claims** — e.g. `a.contains(X) && a.contains(Y)` where `X` is a substring of `Y`, so the conjunction collapses and a second, unrelated arm satisfies it. The second is the hardest to see, because the comment is what a reviewer reads. One remedy covers all: **plant the defect the guard names and watch it redden** — fail-first proof applied to the guard's *stated* property, not merely to its code.

## §4 Invariants with tripwire status (violations die in review)
- A `RefCell` borrow never crosses an `.await`; `std::cell::RefCell` is clippy-banned in the daemon — use the critical-section cell (§16.2).
- `AsyncFd` is banned workspace-wide for pty masters (§15.18; read the P8 annotation before re-litigating).
- `unsafe` lives only in `serial_nexus_sys` (§16.3); everything else is `#![forbid(unsafe_code)]`.
- Every targetward framer fragments oversize chunks via the one shared helper; never skip-on-encode-error (§5, §15.27).
- Purge is one invariant, three instances (§6); the tap/ring mirror never affects `discarded_unattached` (§15.32) — and neither does an armed `tap.wait`, which is the same mirror with a matcher on it.
- The pattern wait's matcher is fed from the hub with **no queue between hub and matcher** (§10), is linear-time and bounded at compile (a backtracking engine on the runtime thread is an operator-reachable DoS, §15.56), resets its lookback window on a feed gap so a match never spans unobserved bytes, and counts toward mirror activity exactly as an open tap does. Expiry is a typed *result*; teardown is a typed *error* (`-32008`). Do not collapse those two.
- Every numeric attribute and every wire-riding identifier carries a stated, structurally checked maximum (§15.34, §16.12).
- The web bridge parses exactly one request per frame and forwards only the allowlist; lifecycle verbs stay off it (§15.34, §15.35).
- Sim doubles are subprocesses, HUP-tolerant, never busy-waiting, idle-CPU asserted (§15.31, §15.36).
- The privileged helper is narrow by construction (§15.45, amended by §15.55): **two** capabilities — `cap_dac_override` for the sysfs write, `cap_fowner` for the ACL grant — argv-only, no environment read, no `exec` while blessed, and its device argument is a kernel-verified sysfs USB **port name**, never a path. Giving it a verb that accepts a filesystem path dissolves every one of those bounds at once and is a design amendment, not a patch. The `grant` verb (§15.55) is that amendment executed the other way: it takes a port name, derives the device nodes itself, grants to `getuid()` and never to an argv uid, and never opens the node. Adding a capability is likewise an amendment — `REQUIRED_CAPS` in `devprep/src/linux/install.rs` is the one place the set is written.

## §5 Documents: amend-first, and the ledgers
When implementation and design disagree, amend the design first (a new §15 entry; annotate superseded entries, never rewrite them), then implement. Deviations land in `docs/implementation-notes.md` §3 as numbered entries. Review findings get a remediation ledger mapping every id to a disposition — including deliberate declines; silently re-fixing a declined item is a defect. Consult the prior reviews' cleared-candidate tables before filing anything.

## §6 Harness doctrine (plan §3)
Fill-then-commit protocol clients (deadline expiry never consumes); completeness asserted only where the design promises it, with `hostward_buffer` provisioned and cited where a test needs it; the loss fingerprint is `received + dropped_slow_consumer == sent`; the sim marks `timed_out` so a deadline is never read as a drop; run quiet and under parallelism; never filter suite output before the failing test's name is captured. When you are validating a **platform** rather than a change, pass `--no-fail-fast`: `cargo test --workspace` stops at the first failing test *binary*, which makes "one known failure" and "four unknown ones" look identical (this cost six consecutive red macOS pushes — `docs/macos.md`, 2026-07-28). **A byte counter is read while the client that fed it is still open** — asserting it after the client's close asserts that the kernel retained the bytes, which Linux does and Darwin does not — measured, not assumed: doctor P13 reads `retains` on Linux 7.0.0-29 (close in tens of µs — 20/10/13 across three captures — 64/64 recovered in all three shapes, `docs/doctor/linux-7.0-2026-08-05-tier3.json`) and `waits-then-discards` on Darwin 24.6.0 (600104 µs, 0 of 64 with no reader; 23 µs, 64 of 64 when the master drains first; 29 µs, 0 of 64 for an `O_NONBLOCK` slave — `docs/doctor/macos-24.6.0-2026-08-05-tier3.json`). So on Darwin such a guard is **not a coin flip but a deadline**: against a blocking slave it has ~600 ms of slack over the daemon's 200 µs–5 ms reader cadence and only reddens on a genuine reader stall, while an `O_NONBLOCK` slave loses the bytes unconditionally in 29 µs — which is why the rule stays absolute rather than becoming "safe on Darwin" (notes §3.29 carries the fix, the rule, and the seven latent siblings; `docs/macos.md`, 2026-08-04).

## §7 Kernel evidence
No one-way decision on single-kernel evidence. When two credible sources disagree about kernel behavior, prefer the kernel-independent design and add a doctor probe (P6–P11 pattern) that measures instead of assumes. A kernel that differs is `degraded` with the observation named, never `unsupported`. Kernel claims in prose cite a committed `docs/doctor/` report (its commit, fingerprint, and date — §16.13), never a terminal scrollback.

## §8 The box, and the tree
Measure the machine before attributing anything to code: load average, competing builds, core count. Flake reproduction rates are meaningless under uncontrolled load. Do not trust `git stash` to bisect a large uncommitted change set — reproduce defects by *reverting the specific fix* in place. Capture failing-test names verbatim before any rerun. A run that died mid-replug leaves a USB adapter deauthorized — it comes back with `./.snx-bin/debug/serial-nexus-devprep authorize --port <p>`, or with a physical unplug/replug, and `serial-nexus-devprep status --port <p>` says which state it is in.

## §9 Verification protocol
A regression guard is valid only with fail-first proof against the unfixed tree. Root-cause claims for flaky or security-relevant findings get an independent adversarial verifier that has **not** read the diagnosis — only the finding and the tree — and the tree must not move while verification runs (the v12 audit returned 35/43 wrong verdicts because it moved). Record refuted diagnoses; they are as load-bearing as confirmed ones. A guard must assert the property the product **promises**, never a proxy for it — in space (a Linux-only observable standing in for a portable one) or in time (a file's existence standing in for a listener's readiness). A proxy is what makes a guard pass on the box it was written on and fail, or worse pass vacuously, everywhere else; the tell that you found the real property is that the portable form comes out *stricter* on the platform of record, not weaker.

## §10 Context hygiene (design §15.41)
Documents, notes, ledgers, reviews, and comments describe *capabilities*, never consumers: no assertions about the existence, count, or nature of external users or any business context. Say "out-of-tree", never `closed-source`, `closed repo`, or `known repository` (gate-banned tree-wide, including `docs/historical/` — the privacy rule outranks the frozen-history rule). `downstream` is allowed only in its data-flow sense; `proprietary` only as a general capability. When quoting older material that violates this, paraphrase.

## §11 Naming
Binaries `serial-nexus-*`; importable crates `serial_nexus_*`; directories short and carrying no family prefix at all — `core/`, `rpc/`, `sys/`, `daemon/` (the library), `daemon-bin/` (the thin binary), `ctl/`, `web/`, `sim/`, `devprep/`, `doctor/`, `itest/`, `codec-api/`, `codecs/reference/`. A gate that scans the filesystem therefore spells the *directory*, never the crate. Old names only in `docs/historical/`, the frozen reviews, and the captured `docs/doctor/` artifacts, enforced by the meta-gate. The extension surface is exactly `serial_nexus_codec_api` plus the `serial_nexus_daemon` entry API, semver'd, proven by the external-consumer template built from the consumer's position on every push.
