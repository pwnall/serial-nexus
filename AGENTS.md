# AGENTS.md — operating manual for serial_nexus implementation sessions

## §1 What this repository is
A Rust workspace implementing the serial_nexus design: `serial-nexus-daemon` (lib `serial_nexus_daemon`), `serial-nexus-ctl`, `serial-nexus-web`, `serial-nexus-doctor`, `serial-nexus-sim`, the `serial-nexus-itest` harness, and the libraries `serial_nexus_core`, `serial_nexus_rpc`, `serial_nexus_sys`, `serial_nexus_codec_api`. Directory names stay short; Cargo `name` fields carry the family prefix (design §15.40). The normative documents are the current design/plan pair named in §2; §ref numbers always point at the current design. Older doc versions, frozen reviews, and remediation ledgers live in `docs/historical/` and are records, not law.

## §2 Current state, and the claims discipline
The normative pair is `41-design-claude-fable-v16.md` + `42-implementation-plan-claude-fable-v16.md`, both directly under `docs/`; when a generation lands, the superseded pair moves to `docs/historical/` and this line and README's documentation index — the two places that name the pair by filename as normative — are bumped in the same commit, together with the §15.41 ban-statement allowance in `itest/tests/meta_names.rs`, which keys on the filenames too. A meta-gate fails if either entry point goes stale. **The v16 generation landed 2026-08-10**: a full rewrite optimized for new developers, produced by a digest fan-out over the v15 pair, the implementation notes, and the review record (17 must-preserve inventories), written to a disposition map that keeps every §15/§16 entry number and every plan §3 rule and §18 item number stable, and adversarially verified by ten independent checkers before landing — the alignment obligation (design front matter, rewrite invariant 2) executed for this generation. Nothing recorded as declined or refuted was silently reversed; overturned declines are marked OVERTURNED at their entries. The design's §1–§14 and §17 are the system as settled, §15–§16 the status-headed decision record; the plan carries the harness doctrine (plan §3, rules 1–21), the testing strategy, and the work ledger. **The plan's Status table is the single home of every current-era measured figure — a figure is quotable only with its scope column — and plan §18 is the canonical enumeration of open work** (of the v15 ledger's fifteen items, 1, 2, 3, 5, 6, 7, 9, 10 and 11 are executed and 4-residual, 8 and 12–15 remain; of the items filed at the rewrite, **23, 32, 33, 34, 35, 37, 39, 40, 43 and 44 were executed 2026-08-12** — notes §3.75 — item 18's suite half is discharged by the same session's CI macOS run (notes §3.76), and 16, 17, 19–22, 24–31, 36, 38, 41, 42 and the newly filed 45 and 46 remain open). **The v16 pair's alignment obligation was executed against the *tree* on 2026-08-12** (the landing had executed it against the documents): 460 normative clauses checked, nineteen deviations confirmed and repaired, two refuted and recorded — two product defects, three gate holes, two design amendments (§15.54), and twelve prose-outran-the-tree corrections. Not one was a rule the rewrite dropped. Kernel claims cite committed `docs/doctor/` artifacts (§16.13); the probe-set era record and the comparability ladder are §13's. The current Linux figure is **890 passing · 0 failed · 6 ignored** at default CI scope and **894 · 1 · 6** on the rig lane (2026-08-12; the rig failure is item 46, and that lane runs without `SNX_RIG_FLOW` because the bench measures 3-wire — notes §3.80) and macOS is **860 · 0 · 6** at whole-workspace scope on the CI arm64 runner — the first green macOS lane in this record — and both are quotable only from the plan's Status table with their scopes. **Do not quote "zero SKIP lines" beside either**: `cargo test` captures a passing test's stderr, so that count is 0 whatever skipped unless the run passed `--nocapture` (notes §3.78; the measured Linux figure is 12). Every headline claim here (suite count, gate set, verified platforms) must match `docs/implementation-notes.md` and the plan's Status table — reviews check this file against reality, and a stale claim is a defect. Update this section in the same commit as the change it describes.
## §3 Gates (all must be green before "done")
`cargo build --workspace --locked` · `cargo test --workspace --locked` · `cargo fmt --all --check` · `cargo clippy --workspace --all-targets -- -D warnings` **and** the minimal-daemon clippy · `cargo deny check licenses bans sources` · macOS cross-check (`--exclude serial-nexus-web`) · the Apple compile cross-check, which the Linux lane now runs on **both** triples (`cargo check --target {aarch64,x86_64}-apple-darwin --workspace --exclude serial-nexus-web --all-targets`) because neither is a superset of the other — each catches the arch-specific code written on the other architecture, and `cargo build` sees neither, compiling no test target (notes §3.71) · `serial-nexus-doctor --json | jq -e -f expectations/linux.jq` (and its macOS-lane counterpart, `jq -e -f expectations/macos.jq`, which the macOS lane actually gates on) — **the `-e` is load-bearing and this line omitted it until 2026-08-07**: without it jq prints `false` and exits **0**, so the command reads as a gate and asserts nothing (measured against a deliberately-gutted report: exit 0 without `-e`, exit 1 with it). CI always had it right, so no lane was ever blind; only this line was — **but the macOS half of that sentence needed a second correction on 2026-08-12**: the gate had not *executed* in any recent run, because the test step ahead of it failed and a failed step skips the rest of the job, and both lanes were piping the doctor into `tee`, which discards its exit status (1 on any `unsupported`, 2 on a report it could not produce). A red check upstream of a gate hides the gate, and `--no-fail-fast` buys that only inside `cargo test` (notes §3.77) · the Playwright gate (self-skips without node) · the meta-gates, including the retired-name grep (§15.40) and the consumer-context grep (§15.41), both in `itest/tests/meta_names.rs`. The **rig lane** is by hand on the crossover box and is not part of the default gate set (CI has no adapters and the tests correctly self-skip): `SNX_CROSSOVER=required SNX_REPLUG=required SNX_TLS=required SNX_RIG_FLOW=required SNX_WEB_UI=required SNX_CROSSOVER_A=… SNX_CROSSOVER_B=… SNX_REPLUG_DEV=/dev/serial/by-id/… SNX_REPLUG_DEV_B=/dev/serial/by-id/… cargo test --workspace --no-fail-fast` (`SNX_WEB_UI=required` joined the documented lane spelling at the v16 landing — notes §3.68 measured the 835 figure with it set, and this line had omitted it; `SNX_REPLUG_DEV_B` joined it 2026-08-12 — notes §3.75 — because without it `identity_survives_a_replug_that_renumbers_the_tty` self-skips and `SNX_REPLUG=required` turns that skip into a hard failure, so the documented lane could not go green on the rig it describes) (`SNX_TLS=required` is the third instance of the one `required` mechanism, covering `web_tls_round_trip`'s two silent skips — no `curl`, and a sandbox that cannot bind `0.0.0.0:0` with `--tls`; plan §3 rule 11, notes §3.57. `SNX_RIG_FLOW=required` is the fourth, and the first whose precondition is *measured* rather than declared: it covers the two `rts-cts` end-to-end tests, which skip on a **3-wire** bench — §5's own stated design assumption, so a rig that answers no is legitimate and `SNX_CROSSOVER=required` deliberately does not redden it (design §15.52, notes §3.63). On macOS `SNX_CROSSOVER` additionally *enables* the `cu.usbserial` scan, which no longer fires unasked — plan §18 item 5) (`--no-fail-fast` per §6 — this is platform validation; **drop `SNX_RIG_FLOW=required` on a bench P5 measures as 3-wire, and `SNX_WEB_UI=required` without `node`** — both are legitimate answers and forcing them fails the lane for the operator's cabling or toolchain, notes §3.80), preceded once by `scripts/bless` (build + install + one `setcap`; `--all` for both profiles). CI enumerates loops from tools (`cargo fuzz list`), never hand-kept lists; meta-gates assert *execution*, not file existence, and a scanning gate proves its **matcher** as well as its walker — plant the violation in every spelling it claims to cover. **The tell for a gate that asserts nothing: its passing output is identical to its not-running output.** Three instances are recorded — `jq` without `-e`, a gate whose step never ran because the step ahead of it was red (notes §3.77), and a SKIP-line grep whose subject `cargo test` captures before it can be grepped (notes §3.78).

## §4 Invariants with tripwire status (violations die in review)
- A `RefCell` borrow never crosses an `.await`; `std::cell::RefCell` is clippy-banned in the daemon — use the critical-section cell (§16.2).
- `AsyncFd` is banned workspace-wide for pty masters (§15.18; read the P8 annotation before re-litigating).
- `unsafe` lives only in `serial_nexus_sys` (§16.3); everything else is `#![forbid(unsafe_code)]`.
- Every targetward framer fragments oversize chunks via the one shared helper; never skip-on-encode-error (§5, §15.27).
- Purge is one invariant, three instances (§6); the tap/ring mirror never affects `discarded_unattached` (§15.32).
- Every numeric attribute and every wire-riding identifier carries a stated, structurally checked maximum (§15.34, §16.12).
- The web bridge parses exactly one request per frame and forwards only the allowlist; lifecycle verbs stay off it (§15.34, §15.35).
- Sim doubles are subprocesses, HUP-tolerant, never busy-waiting, idle-CPU asserted (§15.31, §15.36).
- The privileged helper is narrow by construction (§15.45, amended by §15.55): **two** capabilities — `cap_dac_override` for the sysfs write, `cap_fowner` for the ACL grant — argv-only, no environment read, no `exec` while blessed, and its device argument is a kernel-verified sysfs USB **port name**, never a path. Giving it a verb that accepts a filesystem path dissolves every one of those bounds at once and is a design amendment, not a patch. The `grant` verb (§15.55) is that amendment executed the other way: it takes a port name, derives the device nodes itself, grants to `getuid()` and never to an argv uid, and never opens the node. Adding a capability is likewise an amendment — `REQUIRED_CAPS` in `replug/src/linux/install.rs` is the one place the set is written.

## §5 Documents: amend-first, and the ledgers
When implementation and design disagree, amend the design first (a new §15 entry; annotate superseded entries, never rewrite them), then implement. Deviations land in `docs/implementation-notes.md` §3 as numbered entries. Review findings get a remediation ledger mapping every id to a disposition — including deliberate declines; silently re-fixing a declined item is a defect. Consult the prior reviews' cleared-candidate tables before filing anything.

## §6 Harness doctrine (plan §3)
Fill-then-commit protocol clients (deadline expiry never consumes); completeness asserted only where the design promises it, with `hostward_buffer` provisioned and cited where a test needs it; the loss fingerprint is `received + dropped_slow_consumer == sent`; the sim marks `timed_out` so a deadline is never read as a drop; run quiet and under parallelism; never filter suite output before the failing test's name is captured. When you are validating a **platform** rather than a change, pass `--no-fail-fast`: `cargo test --workspace` stops at the first failing test *binary*, which makes "one known failure" and "four unknown ones" look identical (this cost six consecutive red macOS pushes — `docs/macos.md`, 2026-07-28). **A byte counter is read while the client that fed it is still open** — asserting it after the client's close asserts that the kernel retained the bytes, which Linux does and Darwin does not — measured, not assumed: doctor P13 reads `retains` on Linux 7.0.0-29 (close in tens of µs — 20/10/13 across three captures — 64/64 recovered in all three shapes, `docs/doctor/linux-7.0-2026-08-05-tier3.json`) and `waits-then-discards` on Darwin 24.6.0 (600104 µs, 0 of 64 with no reader; 23 µs, 64 of 64 when the master drains first; 29 µs, 0 of 64 for an `O_NONBLOCK` slave — `docs/doctor/macos-24.6.0-2026-08-05-tier3.json`). So on Darwin such a guard is **not a coin flip but a deadline**: against a blocking slave it has ~600 ms of slack over the daemon's 200 µs–5 ms reader cadence and only reddens on a genuine reader stall, while an `O_NONBLOCK` slave loses the bytes unconditionally in 29 µs — which is why the rule stays absolute rather than becoming "safe on Darwin" (notes §3.29 carries the fix, the rule, and the seven latent siblings; `docs/macos.md`, 2026-08-04).

## §7 Kernel evidence
No one-way decision on single-kernel evidence. When two credible sources disagree about kernel behavior, prefer the kernel-independent design and add a doctor probe (P6–P11 pattern) that measures instead of assumes. A kernel that differs is `degraded` with the observation named, never `unsupported`. Kernel claims in prose cite a committed `docs/doctor/` report (its commit, fingerprint, and date — §16.13), never a terminal scrollback.

## §8 The box, and the tree
Measure the machine before attributing anything to code: load average, competing builds, core count. Flake reproduction rates are meaningless under uncontrolled load. Do not trust `git stash` to bisect a large uncommitted change set — reproduce defects by *reverting the specific fix* in place. Capture failing-test names verbatim before any rerun. A run that died mid-replug leaves a USB adapter deauthorized — it comes back with `./.snx-bin/debug/serial-nexus-replug authorize --port <p>`, or with a physical unplug/replug, and `serial-nexus-replug status --port <p>` says which state it is in.

## §9 Verification protocol
A regression guard is valid only with fail-first proof against the unfixed tree. Root-cause claims for flaky or security-relevant findings get an independent adversarial verifier that has **not** read the diagnosis — only the finding and the tree — and the tree must not move while verification runs (the v12 audit returned 35/43 wrong verdicts because it moved). Record refuted diagnoses; they are as load-bearing as confirmed ones. A guard must assert the property the product **promises**, never a proxy for it — in space (a Linux-only observable standing in for a portable one) or in time (a file's existence standing in for a listener's readiness). A proxy is what makes a guard pass on the box it was written on and fail, or worse pass vacuously, everywhere else; the tell that you found the real property is that the portable form comes out *stricter* on the platform of record, not weaker.

## §10 Context hygiene (design §15.41)
Documents, notes, ledgers, reviews, and comments describe *capabilities*, never consumers: no assertions about the existence, count, or nature of external users or any business context. Say "out-of-tree", never `closed-source`, `closed repo`, or `known repository` (gate-banned tree-wide, including `docs/historical/` — the privacy rule outranks the frozen-history rule). `downstream` is allowed only in its data-flow sense; `proprietary` only as a general capability. When quoting older material that violates this, paraphrase.

## §11 Naming
Binaries `serial-nexus-*`; importable crates `serial_nexus_*`; directories short and carrying no family prefix at all — `core/`, `rpc/`, `sys/`, `daemon/` (the library), `daemon-bin/` (the thin binary), `ctl/`, `web/`, `sim/`, `replug/`, `doctor/`, `itest/`, `codec-api/`, `codecs/reference/`. A gate that scans the filesystem therefore spells the *directory*, never the crate. Old names only in `docs/historical/`, the frozen reviews, and the captured `docs/doctor/` artifacts, enforced by the meta-gate. The extension surface is exactly `serial_nexus_codec_api` plus the `serial_nexus_daemon` entry API, semver'd, proven by the external-consumer template built from the consumer's position on every push.
